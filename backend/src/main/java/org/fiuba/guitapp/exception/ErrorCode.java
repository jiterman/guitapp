package org.fiuba.guitapp.exception;

public enum ErrorCode {
    MAIL_ALREADY_USED,
    USER_NOT_FOUND,
    INVALID_OTP,
    OTP_EXPIRED,
    USER_ALREADY_VERIFIED,
    VALIDATION_FAILED,
    RATE_LIMIT_EXCEEDED,
    UNKNOWN_ERROR
}
